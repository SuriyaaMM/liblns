# Logarthmic Number System Library (liblns)

## Analysis

### LNS8 I4F3 format

### LNS16 I5F10 format

## Installation Instructions
- clone the repository
- install uv (pre-requisite)
- follow instructions
```bash
# create virtual environment (tested with python 3.13)
uv venv --python 3.13
# sync to install dependencies
uv sync
```
